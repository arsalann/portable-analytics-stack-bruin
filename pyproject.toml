[project]
name = "portable-analytics-stack"
version = "0.1.0"
description = "Bruin-powered portable analytics stack for NYC Open Data, DuckLake, R2, and Neon."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.3.0",
    "polars>=1.36.1",
]
